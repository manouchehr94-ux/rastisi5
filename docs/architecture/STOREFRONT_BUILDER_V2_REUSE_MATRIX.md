# Storefront Builder V2 — Reuse Matrix

**Phase:** Audit checkpoint. Classification only — no implementation in this commit.

Classifications used (per the task's Step 19 requirement):
`REUSE_AS_IS` · `EXTEND` · `REFACTOR` · `LEGACY_KEEP` · `REPLACE` · `REMOVE_LATER`

All "Evidence" entries are `SOURCE_ONLY` or `TESTED` (test assertions read, not executed — see the sandbox constraint in `STOREFRONT_BUILDER_V2_EXISTING_CAPABILITY_AUDIT.md` §0). No item below reached `RUNTIME_VERIFIED` or `BROWSER_VERIFIED` this session.

| Capability | Current Implementation | Evidence | Classification | Required Change | Notes |
|---|---|---|---|---|---|
| `StorefrontLayout` | `apps/storefront_builder/models.py` — 1:1 Store anchor, `published_version`/`draft_version` pointers, `uses_visual_storefront_layout` flag | SOURCE_ONLY | REUSE_AS_IS | None for the pointer/flag mechanism itself | This is exactly the "Storefront" root V2 needs; do not create a parallel root model |
| `StorefrontLayoutVersion` | `models.py` — immutable-after-publish, `header_config`/`footer_config`/`appearance_config` JSON, `content_fingerprint`, `Status`/`Source` enums | SOURCE_ONLY | EXTEND | Add `family_slug`/future V2 keys inside the existing `appearance_config` dict (already the pattern used for `template_slug`/`palette_slug`); do not fork a second versioned model | This is the closest existing analog to the spec's "Storefront Layout Version" concept — extend, don't replace |
| `StorefrontSection` | `models.py` — `section_key`, `order`, `is_active`, `collapsed_in_editor`, `settings` JSON | SOURCE_ONLY | EXTEND | Add an optional `page_type`/`page_slug` scoping field (currently implicitly "home only" via the version it belongs to) if V2 needs sections on Product Detail/Listing/Collection/Cart, per spec §5.3 "Page Template" concept | Currently homepage-only; extending to other page types is the single largest schema question for V2 (see Implementation Plan §"Page concept") |
| Section Registry (22 types) | `apps/storefront_builder/section_registry.py` | SOURCE_ONLY | REUSE_AS_IS | None structurally; add new section types additively as needed | Exactly matches spec's "Block Library" allowlist pattern already (`SectionDefinition`-style dataclass, template-name-driven, no dynamic import) |
| Product Section Data Source contract | `section_registry.py` `PRODUCT_SECTION_DATA_SOURCES` + `services/section_data_service.py` | SOURCE_ONLY | REUSE_AS_IS | None | Already satisfies spec §13 "Product Sections... merchant selects data source" |
| Draft lifecycle (`get_or_create_draft`) | `layout_service.py` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None for the pointer-swap mechanism | Idempotent, rate-limited, atomic — matches spec §9 Draft contract |
| Preview | `storefront_preview` view + `render_service.build_render_items` (shared with public render) | SOURCE_ONLY / TESTED | EXTEND | Extend to non-homepage pages if/when Section is extended to other page types (see row above); the *mechanism* (shared render function, staff-only session gate) is reusable as-is | Spec §9 requires "Preview must render the actual Draft through the same universal storefront engine used by public rendering" — already true for Home; not yet true for other pages because they aren't Section-driven yet |
| Publish | `layout_service.publish()` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Atomic pointer-swap, exactly matches spec §9 Publish contract |
| Rollback | `layout_service.restore_version()` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Never publishes directly (creates a new Draft first) — safer than the spec's minimum bar, keep this behavior |
| Published → Draft clone | `layout_service._clone_version_content()` | SOURCE_ONLY | REFACTOR | **Must** be extended to also re-home `HeroSlide`/`PromotionalBanner`/`StoryRailItem` rows onto the new section PKs (remap by `(old_section.section_key, old_section.order)` correspondence, or introduce a stable section identity separate from PK) | See Existing Capability Audit §2 — this is the confirmed, high-priority, documented failure mode (Step 6 of the task). Do not silently patch during this audit checkpoint per instructions; scope this as an early V2/Phase-1-adjacent fix |
| Section-bound media (`HeroSlide`/`PromotionalBanner`/`StoryRailItem`) | `apps/content/models.py`, real FK to `store` + `StorefrontSection` | SOURCE_ONLY | REFACTOR | Needs the clone fix above; also needs a storage-file cleanup hook on CASCADE-delete paths (`discard_draft`/`restore_version`/`apply_industry_layout` all delete the old draft, orphaning uploaded image files with no cleanup, unlike `media_views.py`'s manual delete path) | Two separate, related bugs: (1) lost linkage on clone, (2) orphaned files on CASCADE delete |
| Section ordering | `order` field + atomic reorder/move-up-down services | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Drag-and-drop + explicit fallback buttons already satisfy spec §21 "Responsive Editing... visibility by device" adjacent requirement for non-mouse reordering |
| Section visibility (`is_active`) vs. editor collapse (`collapsed_in_editor`) | `models.py` | SOURCE_ONLY | REUSE_AS_IS | None | Correctly separates "hidden from storefront" from "collapsed in editor UI" — matches spec §8 required operations |
| Section duplication | `storefront_section_duplicate` view, deep-copies `settings` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Confirmed real duplication (independent post-duplicate edits), not a toggle |
| Responsive per-section settings | `partials/section_responsive_fields.html`, `responsive_section_wrapper.html` | SOURCE_ONLY | REUSE_AS_IS | None for the mechanism; extend the *set* of responsive keys per new block types as they're added | Matches spec §21 minimum bar (visibility/columns by device) |
| Appearance config (colors/fonts/radius/density/motion) | `StorefrontLayoutVersion.appearance_config` + `apps/core/context_processors.py` global injection | SOURCE_ONLY | REUSE_AS_IS | None structurally | Already global-scope (applies to every route, not just Builder-aware ones) — this is the one appearance mechanism that's already "universal" the way V2 needs everything to be |
| Legacy `appearance_registry.py` (10 Templates, 20 Palettes) | `apps/storefront_builder/appearance_registry.py` | SOURCE_ONLY | LEGACY_KEEP | Do not delete; do not extend further for V2 purposes | This is a DOM-agnostic, CSS-token-only system, mutually exclusive with `family_slug`. It already satisfies most of V2's "Global Design Tokens" concept (spec §20) for stores that haven't adopted a Family. Keep serving existing stores on it; V2's design-token layer should converge with (not duplicate) this, not `ShopSettings` |
| `theme_presets.py` (6 color-only presets) | `apps/core/theme_presets.py` | SOURCE_ONLY | LEGACY_KEEP | None; note naming collision risk with `preset_registry.py` in any V2 documentation/code | Small, old, unrelated to the Family/Preset system despite the similar name — do not confuse or merge |
| `ShopSettings` (colors, logo, favicon, business fields) | `apps/core/models.py` | SOURCE_ONLY | LEGACY_KEEP | Keep as the pre-Builder-publish color fallback; do not add font/radius/spacing fields here (that already correctly lives in `appearance_config`) | ADR-10 already flagged `ShopSettings`'s 4-unrelated-concerns shape as deferred cleanup — not urgent for V2 |
| Family Registry (11 families, DOM-forking) | `apps/storefront_builder/family_registry.py` | SOURCE_ONLY | LEGACY_KEEP (pending owner decision) | Per spec §23 "Existing Families — Archive Strategy": freeze new family-specific work; do not delete; extract reusable header/hero/card/footer/PDP patterns into V2 blocks/variants/presets over time | This *is* the "unsuccessful long-term architecture" the spec explicitly targets for replacement — but it is recent, heavily tested, and actively used by real stores. Must not be deleted or broken during any V2 work |
| Preset Registry (11 per-family token bundles) | `apps/storefront_builder/preset_registry.py` | SOURCE_ONLY | LEGACY_KEEP (pending owner decision) | Same as Family Registry — extract useful token combinations into V2 global presets rather than deleting | Structurally very close to what spec §22 calls a "Preset" already — good extraction candidate |
| Family/Template mutual exclusivity + `_CandidateAppearanceVersion` in-memory preview shim | `views.py::storefront_appearance_editor`, `~views.py:56-100` | SOURCE_ONLY | REPLACE (in V2) | A unified V2 appearance model should not need this branching; it exists specifically because two historical systems (10 Templates vs. 11 Families) must not both apply at once | Correctly flagged as the most tangled part of the current builder by this session's sub-agent trace; do not extend this branching logic further, even for V2-adjacent work |
| Product card renderer | `apps/catalog/templates/catalog/partials/product_card.html` (dispatcher + legacy fallback + 11 family variants) | SOURCE_ONLY | EXTEND | Convert into a V2 "Product Card" block with configurable density/columns/badge visibility (spec §13), keeping the family variants as selectable presets/variants during transition | Already used consistently everywhere a card renders (home, listing, collection, related products) — good foundation for a universal block |
| Product Detail page | `apps/catalog/templates/catalog/product_detail.html` (dispatcher + 11 family bodies) + `apps/catalog/views.py::product_detail` | SOURCE_ONLY | REFACTOR | Needs to become Section/Block-composed per spec §14, with the 11 family bodies becoming presets/reference material rather than hard dispatch targets | View itself is already family-agnostic (dispatch is template-level) — reduces refactor risk |
| Product Listing / Category page | `apps/catalog/templates/catalog/product_list.html` + `apps/catalog/views.py::product_list` | SOURCE_ONLY | REFACTOR | Needs the shared page shell (`page_shell_header.html`/`page_shell_footer.html`) included, and eventually Section/Block composition per spec §15 | Currently the single biggest concrete example of the "looks like a different storefront" problem — shell is generic `base.html`, only the card partial is family-aware |
| Collection page | `apps/catalog/templates/catalog/collection_index.html`/`collection_detail.html` + `collection_service.py` | SOURCE_ONLY | REFACTOR (page shell) / REUSE_AS_IS (data layer) | Same page-shell gap as Product Listing; the underlying `MerchantCollection` model/service is a genuinely reusable, tenant-safe, ordered-manual-collection data source already | `collection_service.public_collection_queryset`/`collection_visible_items` should be the V2 Collection block's data resolver, unchanged |
| Search results | Reuses `product_list` view via `?q=` query param — no dedicated route | SOURCE_ONLY | REFACTOR | Same page-shell gap; V2 should decide whether Search remains a `product_list` variant or becomes its own Page Template (spec §17) | No dedicated infrastructure to reuse or discard here — it's a query-string branch of an existing view |
| Cart page | `apps/cart/templates/cart/cart_detail.html` + `apps/cart/services/cart_service.py`, `pricing.py` | SOURCE_ONLY | REFACTOR (page shell only) / REUSE_AS_IS (commerce rules) | Page shell needs the same treatment as above; **commerce rules (stock enforcement, gift wrap, coupon, totals, order snapshot) must not be touched or reimplemented by V2** | Spec §14 and §18 are explicit: Builder must consume existing commerce services, never reimplement them. Confirmed correctly isolated already (cart has zero family-DOM coupling today) |
| MerchantCollection / MerchantCollectionItem | `apps/catalog/models.py` + `collection_service.py` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Already a real, ordered, tenant-safe collection model — exactly what spec §5.7/§13 needs as a Data Source; do not build a second Collection concept |
| `storefront_visible_products`/`storefront_listing_products` | `apps/catalog/services/product_publish_service.py` | SOURCE_ONLY | REUSE_AS_IS | None | The correct, only choke point for any V2 product data resolver — spec §30 requirement satisfied already |
| Variant/option-value image switching | `ProductImage.variant`/`ProductImage.option_value` FKs | SOURCE_ONLY | REUSE_AS_IS | None | Data layer already supports what spec §14 calls "Variant Selector... Color Swatches" image behavior; V2 blocks consume, don't rebuild |
| Product video (YouTube/Aparat/Instagram) | `ProductVideo` model + `product_video_service.py` | SOURCE_ONLY | REUSE_AS_IS | None | Matches spec §14's optional video component |
| Compare feature | Not implemented anywhere | SOURCE_ONLY (absence confirmed by exhaustive grep) | — (not applicable — nothing to classify) | Out of scope unless explicitly requested; would be a genuinely new capability, not a V2 reuse question | Platform-wide gap predating this task |
| Sharing | Not implemented anywhere | SOURCE_ONLY (absence confirmed) | — | Same as Compare | Platform-wide gap predating this task |
| Size guide | `ProductMetafield` (namespace `size_guide`) + sanitizer | SOURCE_ONLY (conflicting with a sibling doc's claim — see Existing Capability Audit §8) | EXTEND (pending runtime confirmation) | Confirm at runtime before relying on it; if confirmed working, expose as a V2 Product Detail block setting | Flagged discrepancy, not silently resolved |
| FAQ | Hardcoded demo text in 2 of 11 family templates only | SOURCE_ONLY | REPLACE | A real FAQ block needs a genuine merchant-editable data source (not hardcoded per-family text) if included in V2 scope | Currently decorative, not a reusable capability |
| Menu / MenuItem (navigation) | `apps/content/models.py` | SOURCE_ONLY | REUSE_AS_IS | None for the model; V2 Header/Footer Builder (spec §10.3) should consume this directly | Already store-scoped, already supports Category/Product/Brand/Collection/External destinations + 2-level nesting |
| FooterSettings (legacy, always-on) | `apps/content/models.py` | SOURCE_ONLY | LEGACY_KEEP / eventual REFACTOR | Decide whether V2's Footer Builder (`footer_config` on `StorefrontLayoutVersion`) supersedes this, or the two continue to coexist (one for non-Builder routes, one for the Builder-aware homepage) | Two independently-validated footer toggle systems exist today — a real, previously-undocumented duplication this audit surfaces; needs an explicit owner decision, not a silent merge |
| `footer_config`/`header_config` on `StorefrontLayoutVersion` (Builder-aware) | `models.py`, validated via `layout_service.validate_header_config`/`validate_footer_config` | SOURCE_ONLY / TESTED | EXTEND | This is the more V2-aligned of the two footer/header systems (Draft/Publish/Rollback-aware); extend it to be included on every route (not just Home/Preview) as part of Phase 1 | Already enforces non-removable elements (cart link, home link, ≥1 active footer column) — good foundation |
| Builder merchant UI — section CRUD (add/remove/toggle/collapse/duplicate/reorder/move) | `apps/storefront_builder/views.py` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None structurally | Directly reusable behind a redesigned single-screen UI; the backend contract doesn't need to change for a new frontend |
| Builder merchant UI — settings-form-per-section-type | `storefront_section_settings` view, one large `if/elif` chain keyed on `section_key` | SOURCE_ONLY | REFACTOR | A V2 "Settings Inspector" (spec §6) wants a declarative/schema-driven form generator, not a growing branch chain | Correctly flagged by this session's sub-agent as the least scalable part of the current settings UI |
| Builder merchant UI — appearance editor (Family/Template/Preset/Palette hub) | `storefront_appearance_editor` view | SOURCE_ONLY | REPLACE (in V2) | Same reasoning as the registry-level row above | High coupling to the legacy dual-system; do not extend further |
| Builder Preview mechanism (staff-session, iframe, postMessage bridge for click-to-select/drag-reorder) | `preview.html`, `editor.html` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Genuinely good pattern for V2's "Live Canvas" (spec §6); the postMessage bridge and device-toggle CSS-class approach transplant directly |
| Undo/Redo | Not implemented | SOURCE_ONLY (absence confirmed) | — | New capability if required; not a reuse question | Confirmed absent by the Builder-UI sub-agent (no dedicated test file, no code) |
| Scheduled Publish | Not implemented | SOURCE_ONLY (absence confirmed) | — | New capability if required; `enforce_rate_limit`-style existing cron/command patterns (e.g. `expire_inventory_reservations`) are the right infrastructure precedent if built | Same |
| Shareable Preview link/token | Not implemented (session-only) | SOURCE_ONLY | — | New capability if required; `apps/portal/services/handoff_service.py`'s `TimestampSigner` pattern is the only existing signed-token precedent in the repo | Explicit prior owner decision recorded in code comments: staff-only, not shareable, by design — revisiting this needs an explicit new owner decision, not an assumption |
| Rate limiting on Publish/Reorder | `apps/core/services/rate_limit.py::enforce_rate_limit` | SOURCE_ONLY / TESTED | REUSE_AS_IS | None | Generic, already used elsewhere in the platform (SMS, portal) |
| Tenant isolation (all models reviewed) | Various — direct `store` FK or verified transitive chain | SOURCE_ONLY / TESTED | REUSE_AS_IS | None found requiring change | Cart/Wishlist's service-layer-only scoping (no direct FK) is a documented pattern to preserve carefully, not a gap to fix in V2 |
| Storefront render caching | Absent everywhere | SOURCE_ONLY (absence confirmed) | — | Out of scope for V2 Phase 1 per spec's own "avoid premature optimization" guidance (§32); revisit later | Confirmed still absent (no `CACHES` override, no `cache_page` anywhere) |
